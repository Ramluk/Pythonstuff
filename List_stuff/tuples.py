#like lists, but immutable values, an immutable list is a tuple.
#for a rectangle that needs to have certain dimensions
dimensions = (200,50)
print(dimensions[0])
print(dimensions[1])
#you can rewrite the tutple
dimensions = (50,400)
for i in dimensions:
    print(i)